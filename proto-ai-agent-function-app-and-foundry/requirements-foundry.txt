# Base agent dependencies
-r requirements.txt

# Foundry Hosted Agent HTTP host (Invocations protocol)
fastapi>=0.115
uvicorn[standard]>=0.30

# Foundry model gateway (models from the Foundry catalog) + Entra ID auth.
# Used only when MODEL_PROVIDER=foundry.
azure-identity>=1.17
azure-ai-projects            # provides AIProjectClient.get_openai_client(); pin to the
                             # version that matches your Foundry project if needed.
